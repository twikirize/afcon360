# Global Agent Routing

Apply `agent-governance-rules.md` for task classification, adaptive context
loading, proportional verification, and conditional durable-document updates.
The root `AGENTS.md` remains authoritative; this file only routes agents to
the distributed execution procedure.