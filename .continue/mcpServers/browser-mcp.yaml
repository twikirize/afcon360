name: Browser search
version: 0.0.1
schema: v1
mcpServers:
  - name: Browser search
    command: cmd
    args:
      - "/c"
      - "npx"
      - "-y"
      - "@playwright/mcp@latest"
      - "--browser=chromium"
    connectionTimeout: 180000
    env: {}