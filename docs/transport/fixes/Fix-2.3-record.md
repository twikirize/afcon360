# Fix 2.3 — Record

Status:          PASS
Commit:          <filled after local commit>
Date:            2026-09-22
Owner:           agent + human (migration executed by human)

Files changed:
  - app/transport/models.py                                      (+6)
  - app/transport/services/reservation_service.py                (+21)
  - tests/integration/test_reservation_callback_idempotency.py   (new, 3 tests)
  - migrations/versions/e6f319ffc8c8_reservation_wallet_ref_unique.py  (human-generated, applied)
  - docs/transport/fixes/Fix-2.3-contract.md                     (new)
  - docs/transport/fixes/Fix-2.3-evidence.md                     (new)
  - docs/transport/02-OPERATING-SYSTEM.md                        (OS replacement + scope-rule bullets)
  - docs/transport/00-MANIFESTO-EDITION-2.0.md                   (+4, Fix 2.3 entry)

Evidence:
  See docs/transport/fixes/Fix-2.3-evidence.md

Runtime verification:
  `& .venv/Scripts/python.exe -m pytest tests\integration\test_reservation_callback_idempotency.py -v --no-header`
  → 3 passed, 4 warnings in 15.51s
  Index confirmed in the database (read-only):

    CREATE UNIQUE INDEX ix_reservation_wallet_ref ON public.transport_reservations
    USING btree (wallet_transaction_reference) WHERE (wallet_transaction_reference IS NOT NULL)

  alembic_version = e6f319ffc8c8 (head); legacy transport_bookings_assigned_driver_id_fkey
  dropped by migration (redundant — fk_booking_driver remains, ON DELETE SET NULL).

Residual risk:
  The uniqueness of wallet_transaction_reference is a partial index, so a NULL reference
  is still replayable. The same-row replay guard (row.wallet_transaction_reference ==
  incoming) covers the same reservation; a cross-reservation duplicate with a NULL
  reference is not blocked, and a terminal reservation receiving a replayed reference
  whose target differs from the current obligation still raises the pre-existing B-3
  guard rather than no-op. No test covers the cross-row IntegrityError branch directly
  (it would require a deliberately conflicting reference); it is exercised only via the
  exception handler code path review.

Follow-ups:
  - Cross-row same-reference collision test (integrity-error branch) — deferred, fix's
    own test file only.
  - Assertion in Fix 2.3 §6: contract's duplicate-proof query may be run by the human
    against afcon360_test for extra evidence.

Migration:
  Authorized by the Fix 2.3 contract. Generated by the human via `flask db migrate`
  (head e6f319ffc8c8, down_revision 1790097621) and applied via `flask db upgrade`.
  Agents did not author or edit the migration file; the autogenerate snapshot included
  the redundant FK drop (transport_bookings_assigned_driver_id_fkey) which was accepted.

Gate reference:
  Edition 2.0, Fix 2.3 — Reservation callback idempotency