# AFCON360 Universal Agent Activity & Implementation Log

> **MANDATORY INSTRUCTION FOR ALL AGENTS**: This file is the absolute source of truth for all codebase modifications, architectural shifts, and feature implementations. Every agent MUST:
> 1. **Read this log** before starting any task to understand current system state.
> 2. **Update this log** immediately after completing a task with a concise summary of changes.
> 3. **Maintain the "Directives" section** if new project-wide rules are established.

---

## 🚀 Unified Module Toggle System (Completed: 13 June 2026)
**Owner**: Gemini CLI Agent
**Status**: STABLE

### 💎 Summary of Work
Transitioned the platform from inconsistent config-based toggles to a DB-backed, owner-controlled feature flag system.

**Key Changes**:
- **Source of Truth**: PostgreSQL via `SystemConfig` model (`MODULE_FLAGS` key).
- **Lookup Path**: `module_enabled('name')` helper in `app.utils.module_guard`.
- **Consistency**: `reload_modules` middleware refreshes `app.config` from DB per-request.
- **Security**: Toggling restricted to **Owner** and **Super Admin** roles.
- **UI**: Standardized all templates to use `{% if module_enabled('name') %}`.

### ⚠️ Reverted Experiment (Over-Hardening)
Attempted to introduce thread-safe TTL locking and 503 status codes. This was **REVERTED** due to excessive complexity and template rendering breakages. Future agents should prefer the current simplified middleware-sync approach unless performance dictates otherwise.

---

## 📝 Universal Directives for Future Agents
*These rules apply to all work done in this repository.*

1. **Module Gating**: Always use the `module_enabled()` helper from `app.utils.module_guard`. Never read raw config flags.
2. **Database Integrity**: When modifying `SystemConfig`, ensure changes are reflected in the corresponding service layer.
3. **Template Reliability**: Avoid complex dictionary `.get()` logic in Jinja2 templates; use established Python helpers to ensure UI stability.
4. **Documentation**: Every major workstream MUST have an entry in this file.

---

## 🛠️ Upcoming Workstreams / Roadmap
*Add new planned items here.*

### GEO Planning Reconciliation (completed)

1. [COMPLETED] NODE 1 — GEO FOUNDATION + CONTROL-PLANE INTEGRATION: PASS
   - `app/geo/` package created with interfaces, services, validation, routes, tests
   - Module registry: `geo` added to `MODULE_REGISTRY`
   - Config: `geo: True` in `MODULE_FLAGS` (base + TestingConfig)
   - Owner/Super Admin toggle controls for GEO
   - `/geo/` and `/geo/health` routes with proper guards
   - 47/47 tests passing without PostGIS

2. [DEFERRED] NODE 2 — POSTGIS ARCHITECTURAL RECONCILIATION: DEFERRED
   - PostGIS not a current implementation dependency
   - Option A (scalar lat/lng) chosen over B/C (PostGIS geometry/geography)
   - No migration authorized; no Docker/PostGIS installation authorized
   - PostGIS remains future architecture decision
   - Minimal change plan: no code changes required; 47/47 tests pass without PostGIS

3. [COMPLETED] NODE 3 — GEO CONSUMER ADOPTION & FRAGMENTATION RECONCILIATION: PASS
   - Transport vs GEO policy split classified (business policy MAY REMAIN LOCAL; generic GEO capability SHOULD MIGRATE)
   - Accommodation vs GEO policy split classified (business policy MAY REMAIN LOCAL; generic GEO capability SHOULD MIGRATE)
   - Events spatial paths inspected: no reusable GEO capability
   - Booking coverage corrected (found in accommodation/transport/events domains, not a standalone `app/booking/` directory)
   - Transport radians defect documented as ACTIVE DEFECT (NOT FIXED in this node)
   - Transport matching numerical regression coverage: NOT VERIFIED
   - Fragmentation classification with ownership boundaries
   - Consumer inventory completed for all inspected domains
   - 47/47 tests passing

4. [COMPLETED] GEO-13 — MAP RENDERER INTEGRATION: PASS
   - Shared renderer boundary (`app/geo/map_renderer.py` + `static/js/geo/geo-map.js`); `/geo/` overview renders Leaflet over public OSM raster fallback; 9/9 tests; no MapLibre, no paid APIs
5. [COMPLETED] GEO-14 — REALTIME LOCATION DELIVERY: PASS
   - Envelope/publication/SSE boundary (`app/geo/realtime.py`, `/geo/stream/location/<type>/<ref>` admin-guarded); producer hook in `TrackingService.update_location`; shared `geo-realtime.js` client consumed by Transport driver location page (CASE C route repair); 30/30 tests; browser-proven live/stale/missing
6. [COMPLETED] GEO-15 — LOCATION HISTORY & RETENTION (foundation): PASS for authorized scope
   - Durable `geo_location_observations` via autogenerated migration `6f8f4c204b61` (AGENTS.md §20.1 rule added to both governance sources first); same-transaction writer; admin-guarded history retrieval; 15/15 tests; GEO-13/14 intact (39/39)
   - OPEN (not downgrading): retention policy = PENDING PRODUCT/COMPLIANCE DECISION; sampling = EVIDENCE-GATED; no legitimate existing history UI consumer (no UI invented). See BACKLOG closeout entries.
7. [COMPLETED] TRANSPORT BOOKING SHOW 500 REPAIR: PASS
   - Root cause: phantom template key (`scheduled_pickup_time` never existed as column/property/serializer); `|datetimeformat` runs before `|default` so the guard never applied. Fix in one layer: `Booking.to_dict()` alias = required `pickup_time` ISO-8601 (include/exclude honored). No template change, no migration.
   - 3/3 new tests; browser Cases A/B/C (200, correct value, absent datetimes render empty, desktop overflow 0; mobile 409px pre-existing shell debt recorded for UX checkpoint); serialization/API + D5/tracking/matching green (4 pre-existing moderator failures unchanged); GEO suite green.
   - BACKLOG defect marked Done; Transport UX Review checkpoint recorded (DEFERRED, not executed).
8. [COMPLETED] PASSENGER BOOKING-SCOPED LIVE TRACKING: PASS
   - Transport-owned `get_rider_tracking_subject` (owner-or-admin, canonical active states, terminal/released/missing deny) + `GET /geo/stream/booking/<ref>` SSE with per-tick revalidation (`closed` frame, reconnect re-authorizes) + show-page Live Tracking card (shared GeoMap/GeoRealtime, server flag, jsdelivr CSP pattern, `closed` client mapping).
   - 20/20 matrix tests incl. mid-stream completion race; browser: rider live + marker move, second-rider page+403 denial, stale/missing/disconnect/reconnect-recovery/mid-stream Trip-ended; 266-test regression green; no migration.
   - BACKLOG tracking GAP marked Done. No global maps, no admin-auth weakening, no new infra.
9. [COMPLETED] TRANSPORT UX READINESS AUDIT (evidence-only): PASS as audit
   - Front page/transport entry/rider form/driver workspace/ops audited in browser + code; journey proven where working.
   - P0: driver-dashboard 500 (concurrent template move; recorded, untouched). P1: no pre-submit fare preview; KYC ribbon intercepts context-switch clicks; driver 403 dead-end post-login. P2: fleet map, waiting copy, switch payload, shell mobile overflow. P3: front H1, icon-button names.
   - No code changed except reverting two concurrent regressions in app/__init__.py (template path + TESTING re-raise) with suite recovery as proof. No migration. Findings folded into BACKLOG checkpoint entry; no new UX items beyond the P0 defect.
10. [COMPLETED] P0 DRIVER DASHBOARD 500 REPAIR: PASS
   - Ownership proven first: route owner Transport + template owner Driver Workspace agree (COHERENT direction, broken reference). Fix: render path to moved template + removed dangling vehicle_marketplace anchor (endpoint never existed).
   - 3/3 new tests (real switch-context login, 200 + sections, 403 without context); browser driver login -> switcher -> workspace 200 with all sections; 390/412/desktop overflow 0; journey + GEO suites green (4 pre-existing moderator failures unchanged); STARTUP_OK.
   - BACKLOG P0 marked Done. No redesign, no KYC/pricing/marketplace/GEO changes, no migration.
11. [COMPLETED] P1 DRIVER 403 / CONTEXT-SWITCH RECOVERY: PASS
   - Root cause: login resolver sent verified drivers to the dashboard without establishing Driver context (guard correct, redirect incoherent, bare-403 dead end). Fix in `_dashboard_for_user` STEP 7: auto-establish the single eligible Driver context via the existing switch service (same validation); failures fall through to user dashboard.
   - 5/5 new tests (real login POST -> workspace 200, unverified/non-driver/anon/invalid denied); browser login -> workspace with all sections; 390/412/desktop overflow 0; 86-test auth/context/GEO regression green; STARTUP_OK.
   - BACKLOG checkpoint updated (P1 resolved; fare preview + ribbon remain open). No KYC/go-live/approval changes, no migration.
12. [COMPLETED] FARE ENGINE + PRE-SUBMIT PREVIEW: PASS
   - ONE canonical engine (`fare_service.py`, FARE_VERSION=1): BookingService estimate + payment final delegate with identical math; dead PaymentService shadow family deleted (zero callers); surge now recorded at creation; negative-distance clamp (integrity).
   - Preview: `POST /api/transport/fare/estimate` (login, 401-correct anon handling) + booking-form estimate card (server-computed breakdown, planning-distance labeled, honest failure copy). No JS pricing.
   - 14/14 new tests (exact values, delegation consistency, endpoint auth/basis/currency, no ID leaks); browser estimate + reactivity + mobile + clean console; 161-test fare/booking/payment/GEO regression green (4 pre-existing moderator failures unchanged); STARTUP_OK; no migration.
   - BACKLOG: fare-governance + un-geocoded-booking gaps recorded (deduped); P1 preview marked shipped. No admin UI/audit/versioning built (governance node), no promo engine, no surge-tz change, no FX.
13. [COMPLETED] FARE GOVERNANCE & CONFIGURATION: PASS
   - Tables in TransportSetting `fare.tables` (pricing, permission-gated, non-public); versions + effective_from (staged futures supported); audit via modification_history (repaired pre-existing update_setting crash on missing app.core.logging); cache invalidated on write; engine/preview/final resolve effective version; historical explainability without per-booking stamp or migration.
   - Endpoints GET/PUT /api/transport/fare/config gated by transport.settings (owner unconditional, delegated-only). 14/14 governance tests (full auth matrix, versions, futures, audit, cache, no-ID-leak); browser owner PUT->version->preview-reflects.
   - 163-test regression green (4 pre-existing moderator failures unchanged); engine 14/14 intact; STARTUP_OK; BACKLOG governance entry Done. No admin UI, no reason field (schema limitation recorded), no migration.

### Non-GEO Workstreams (existing)

1. [Planned] Audit and consolidate all remaining module-specific decorators into `app.utils.module_guard`.
2. [Planned] Standardize all error pages (403, 404, 500) to match the new `module_disabled` aesthetic.

---
*Log initialized by Gemini CLI Agent on Saturday, 13 June 2026.*
